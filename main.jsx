import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <h1 style={{color: "white", background: "#121212", padding: "20px"}}>Journalses App (Demo)</h1>
  </React.StrictMode>,
)
